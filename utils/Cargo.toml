[package]
name = "utils"
version = "0.1.0"
edition = "2021"
publish = false


[dependencies]
migration = { path = "../migration" }

axum = { workspace = true }
tower = { workspace = true }
sea-orm = { workspace = true, features = ["sqlx-sqlite"] }
